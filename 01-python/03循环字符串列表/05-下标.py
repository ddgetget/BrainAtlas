# 下标： 其实就是一个编号，可以根据下标获取对象(字符串，列表， 元组)中某个数据

# 提示： 在python里面下标可以是正数也可以是负数 0表示第一个数据， -1： 表示倒数第一个数据

my_str = "hello"


# 根据正数下标获取指定的数据
result = my_str[4]
print(result)


# 根据负数下标获取指定的数据
result = my_str[-1]

print(result)

result = my_str[-5]
print(result)